# 第 52 轮（2026-09-20）— 全库超级深度 + 超级广度扫描：发现一个修一个 → 版本 2.4.5

## 0. 一句话结论

基线 `cargo check --no-default-features --features desktop --all-targets` 为 `Finished`，
全量测试 `5300 passed / 0 failed`。本轮从**平台探针真实性**、**数值边界崩溃**、
**跨目标门控**、**控件 enabled 契约**四个方向取证，挖出 **5 大类、14 个真实缺陷**，
其中两个是**必然触发的进程级 abort** 与 **Apple 平台全线返回 None 的假探针**。

本轮最重要的产出：

1. **`cupertino_slider` 一次属性写入即 abort 进程** —— `f32::clamp(min, max)` 在
   `min > max` 时 panic。这不是极端输入：`min`/`max` 都是公开 setter 与可写
   capability 属性，`write_property(w, "min", 3.5)`（JSON 声明式层、C ABI、所有语言
   绑定都走这条路）在没有先设 `max` 的情况下**直接杀死宿主进程**。同一模式在
   **13 个其他控件**中存在（slider / meter / arc / scrollbar / progressbar / arc /
   dial / spinbox / stepper / lcd_number / progress_dialog / range_slider / video seek）。
2. **Apple 四后端的内存/电量探针全部接错内核** —— cocoa / objc2 / iOS / mobile 四个
   后端把 `total_memory_mb` / `is_on_battery` / `process_memory_utilization` 转发到
   `os_probes`，而该模块解析的是 `/proc/meminfo`、`/sys/class/power_supply`、
   `/proc/self/status`——**只有 Linux 内核才有**。在 Apple 机器上四项全部静默降级为
   `None`/`false`，本机实测确认。
3. **`mini` + OpenHarmony 完全不编译**（4 个 error），而 `check_harmony_cross.sh`
   只跑 `embedded`，因此门禁从未覆盖。
4. **WASM 后端谎报能力**：`family()` 返回 `Desktop`，于是 trait 默认让一个浏览器页面
   声称自己拥有 DPI 缩放、IME、无障碍树和原生菜单栏；而 README 一直印着 `Embedded | ❌❌❌❌`。

---

## 1. 方法论（原则 #1/#16/#56）

1. 先跑基线：`cargo check --no-default-features --features desktop --all-targets` → `Finished`。
2. 六路并行只读取证（子代理，各自 disjoint 范围），每条要求 `file:line` + 证据。
3. **逐条复核子代理结论，不直接采信**。本轮复核推翻了两条：
   - 「147/187 控件忽略 enabled」——实测 grep 出的 `impl Draw` 文件里没有
     `is_enabled()` 的是 **32 个**，且其中多数是容器/图表（`enabled` 对其语义上
     不适用）。子代理的数字把「未引用」与「本应引用」混同了，实际需要修的是
     交互型控件。**记录为误报并按下述重新界定范围。**
   - 「WASM `total_memory_mb` 等是假探针」——复核后确认它们是**诚实返回 None**，
     不是假探针；真正的缺陷是 `family()` 与 `capabilities()`。
4. 每条修复都做**反向注入验证**：把修复去掉 → 观察测试按预期失败 → 恢复修复 → 观察通过。
5. 逐轮只跑「定点证据」，全量门禁留到最后一次性跑（原则 #56）。

---

## 2. 🔴 头条 A：一次属性写入 abort 宿主进程（`f32::clamp` 边界）

### 2.1 取证（修复前的实测输出）

```
$ cat /tmp/rwprobe/src/main.rs    # 走公开 API，与 JSON / C ABI 同一条路
let factory = WidgetFactory::new_with_defaults();
let mut w = factory.create("cupertino_slider", Rect::new(0,0,200,40), "").unwrap();
println!("max before = {:?}", factory.read_property(w.as_ref(), "max"));
factory.write_property(w.as_mut(), "min", CapabilityValue::Float(3.5))

$ cargo run --quiet
created; kind=CupertinoSlider
max before = Ok(Float(1.0))

thread 'main' panicked at core/src/num/f32.rs:1566:9:
min > max, or either was NaN. min = 3.5, max = 1.0
```

### 2.2 为什么这是**必然**而非极端

`cupertino_slider` 的构造值是 `(min, max) = (0.0, 1.0)`，而 `min` 是
`PropertySchema::new("min", Float, true, true)`（可写），`set_min` 的文档明确写
"re-applying the clamp to the current value"。因此**两个文档化的公开调用按任意顺序
执行**就会 abort。参数顺序不是 GUI 库可以附加的前置条件。

### 2.3 同类模式（冰山法则）：13 处，不是 1 处

`grep` 出所有「界限来自两个**可变字段**」的 clamp 点，逐个确认不是常量界限：

| 控件 | 文件 |
|---|---|
| `cupertino_slider` | `cupertino/core.rs` |
| `lcd_number` | `display_widgets/lcd_number.rs` |
| `meter`（3 处） | `display_widgets/meter.rs` |
| `scroll_bar`（2 处） | `display_widgets/scrollbar.rs` |
| `arc`（2 处） | `display_widgets/arc.rs` |
| `slider`（3 处） | `display_widgets/slider.rs` |
| `progress_bar` | `display_widgets/progressbar.rs` |
| `progress_dialog` | `dialog/progress_dialog.rs` |
| `stepper` | `container_widgets/stepper.rs` |
| `spin_box` | `input_widgets/spinbox.rs` |
| `dial` | `advanced_widgets/dial.rs` |
| `range_slider`（6 处） | `input_widgets/range_slider.rs` |
| `VideoEngine::seek` | `video/engine.rs`（`duration` 是 **public 字段**，容器解析失败可为 `NaN`） |

### 2.4 修复：单一接缝 `src/widget/numeric.rs`

不逐个控件写 `if min > max { swap }`（14 份会漂移），而是新增
`src/widget/numeric.rs`，提供 `ordered_clamp` / `_f64` / `_i32` / `_u32` / `_usize`：

- 把界限当作**无序的一对**，取小者为下界 → 交叉界限得到夹紧值而非 panic；
- 任一边界为 `NaN` 时**忽略该边界**（`NaN` 不是区间端点）；两边都是 `NaN` 时原样返回；
- 声明顺序下与 `std::f64::clamp` **逐位一致**（有测试钉住），因此对正确调用者是
  零行为变更的 drop-in 替换。

### 2.5 定点证据（修复后）

```
$ cargo run --quiet          # 同一探针
created; kind=CupertinoSlider
max before = Ok(Float(1.0))
write min=3.5 -> Ok(())      # 不再 panic

$ cargo test --no-default-features --features desktop --lib cupertino
running 57 tests ... test result: ok. 57 passed

$ cargo test --no-default-features --features desktop --lib widget::numeric
running 6 tests ... test result: ok. 6 passed
```

### 2.6 反向注入验证（原则 #12）

把 `cupertino_slider` 的 `ordered_clamp` 改回 `.clamp`：

```
test widget::cupertino::core::tests::cupertino_slider_min_above_max_clamps_instead_of_panicking ... FAILED
thread 'main' panicked at core/src/num/f32.rs:1566:9:
min > max, or either was NaN. min = 3.5, max = 1.0
test result: FAILED. 0 passed; 1 failed
```

恢复修复后该测试通过。**测试确实能捕获该缺陷，不是恒真断言。**

---

## 3. 🔴 头条 B：Apple 四后端探针接错内核 → 全线返回 `None`

### 3.1 取证（修复前，本机 macOS 实测）

```
$ cargo run --quiet            # 探针直接调用 Platform trait
backend=cocoa
family=Desktop
total_memory_mb=None
is_on_battery=false
process_memory_utilization=None
process_cpu_utilization=None
```

四后端（`macos/`、`macos_objc2/`、`ios/`、`mobile.rs`）的四个探针**全部**转发到
`crate::platform::os_probes`。该模块的模块文档自己写明：

> Linux-kernel system probes shared by every `/proc`- and `/sys`-based backend.

它解析 `/proc/meminfo`、`/sys/class/power_supply`、`/proc/self/status`。
**Apple 内核不挂载这三个文件**，于是每次查询都走 `ok()?`/`let Ok(..) else` 的
降级分支，返回 `None`/`false`。

### 3.2 为什么长期无人发现

因为 `None` 和 `false` 是**合法返回值**。`Platform::total_memory_mb` 的文档甚至
主动要求「必须把 `None` 当作未知，绝不允许用 `4096` 这类编造值替代」——于是
「诚实返回 None」与「探针接错内核所以永远 None」在**返回值上不可区分**，
没有任何断言会失败。而 trait 的同一份文档又承诺 macOS 走 `sysctl hw.memsize`、
进程内存走 `ps`——**文档与代码相反**（原则 #18）。

### 3.3 修复：新增 `src/platform/darwin_probes.rs`

按内核分层，与 `os_probes`（Linux 内核）并列：

| 函数 | 实现 | 依据 |
|---|---|---|
| `total_memory_mb` | `sysconf(_SC_PHYS_PAGES) × _SC_PAGESIZE` | 与 `sysctl hw.memsize` 同量同义，且无需额外 FFI 绑定 |
| `is_on_battery` | `pmset -g batt` 是否含 `'Battery Power'` | 系统自带；`'AC Power'`（台式机）与 Linux 空目录同答 `false` |
| `process_memory_utilization` | `ps -o rss=,vsz= -p <pid>` | 与 Linux 的 `VmRSS/VmSize` 同义同比例 |
| `process_cpu_utilization` | 返回 `None` | macpset/ps 无稳定的一次性采样；**不编造** |

`libc` 依赖加在 `[target.'cfg(target_os = "macos")']` 与 `ios` 段（**非 optional**：
无条件编译的探针挂在 optional 依赖上就是下一次编译错误的种子）。

### 3.4 定点证据（修复后，同一探针）

```
backend=cocoa
total_memory_mb=Some(16384)          # ← 之前是 None
is_on_battery=true                   # ← 之前是恒 false（本机确实在用电池）
process_memory_utilization=Some(1.2663635e-5)   # ← 之前是 None
process_cpu_utilization=None         # 诚实：无稳定来源
```

16 GiB / 电池状态 / RSS 比例均为**真实测量值**，非编造。

### 3.5 新增测试（防回归）

`src/platform/darwin_probes_tests.rs`，5 条，**全部钉住"测量值"而非"无 panic"**：

| 测试 | 断言 |
|---|---|
| `total_memory_is_measured_and_plausible` | 返回 `Some`，且在 256 MiB–4 TiB 之间 |
| `process_memory_utilization_is_a_fraction` | 返回 `Some`，且在 `(0, 1]` |
| `battery_answer_matches_the_pmset_banner` | 重新跑 `pmset` 并要求探针与横幅**一致**（不假设本机电源状态） |
| `ps_selector_is_bsd_spelled` | `["-o", "rss=,vsz="]` 且 `ps` 输出的首个 token 可解析为数字 |
| `parse_error_message_names_the_offending_output` | 失败信息含原始输出与期望字段名 |

第 3 条的写法值得说明：它不硬编码 `true`/`false`（那会让测试依赖本机是否笔记本），
而是**重新执行文档化的命令并比对**——钉住「答案来自 `pmset`」这一契约。

---

## 4. 🔴 `mini` + OpenHarmony 不编译，且门禁从未覆盖

### 4.1 取证

```
$ cargo check --target aarch64-unknown-linux-ohos --no-default-features --features mini
error[E0425]: cannot find type `String` in this scope
  --> src/platform/harmony/platform_impl.rs:59:73
error[E0425]: cannot find type `String` in this scope
  --> src/platform/harmony/platform_impl.rs:131:50
error[E0425]: cannot find type `String` in this scope
  --> src/platform/harmony/platform_impl.rs:155:37
error: could not compile `rust_widgets` (lib) due to 3 previous errors
```

`mini` 下 crate 是 `#![no_std]`，std prelude 消失；其余每个后端都显式
`use crate::compat::{format, String}`（windows/linux/ios/android 都有），
**只有 harmony 漏了 `String`**。

### 4.2 门禁缺口

`tools/check_harmony_cross.sh:168` 只跑 `--features embedded`，
因此 `mini` × OpenHarmony 这一组合从未进过 CI。

### 4.3 修复与定点证据

```rust
use crate::compat::{format, String};
```

```
$ cargo check --target aarch64-unknown-linux-ohos --no-default-features --features mini
    Finished `dev` profile ... (0 error, 0 warning)
```

---

## 5. 🔴 WASM 后端谎报能力，且门禁结构上抓不到

### 5.1 取证

`src/platform/wasm/platform_impl.rs` 原为：

```rust
fn family(&self) -> PlatformFamily {
    PlatformFamily::Desktop
}
```

`capabilities()` 未覆写，于是走 `types.rs` 的默认实现——**该默认按 family 推导每个
flag**，`Desktop` 得到四个 `true`。运行时实测：

```
family=Desktop
capabilities=PlatformCapabilities { dpi_scaling: true, ime: true, accessibility: true,
                                    native_menu: true, typed_widget_trigger: true }
```

而 `README.md:232` 一直印着：

```
| **Web (WASM)** | `wasm-state-backend` | Embedded | ❌ | ❌ | ❌ | ❌ | ❌ |
```

即：**文档说没有，代码说有**。且同族的另一个无主机后端 `portable/mod.rs:134` 明确
写着 `FAMILY = Embedded` 并附理由「报告 Desktop 会让自适应代码为无法兑现的主机选择
桌面默认值」——wasm 恰好做了它警告的事。

### 5.2 为什么门禁没抓到（根因，比缺陷本身更重要）

`src/docs_paths_tests.rs` 的 `published_os_capability_matrix_matches_the_trait_default`
**从不构造任何后端**。它把 README 声明的 family 喂给
`default_capabilities_for(family)` 造一个**合成结构**，再拿它和 README 比对：

> 把声明当作自己的期望值 —— 这种断言**在结构上无法被证伪**。

所以 README 的 `Embedded` 一行被原样回喂，测试永远通过。

### 5.3 修复（两层）

**(a) 后端说真话**：`family()` → `Embedded`，并**显式覆写** `capabilities()` 给出四个
`false`。显式而非继承，是为了让未来改动 `default_capabilities_for` 不能悄悄重新授予
浏览器没有的能力。

**(b) 门禁通过构造真后端来验证**：新增
`documented_matrix_matches_real_backends`，**真的 `Box::new(...)` 构造本机可构造的
每个后端**，调用 `capabilities()`/`family()`，与按 `backend_name()` 索引的 README 行
逐字段比对；无法构造的后端（别的 OS / feature 关掉）按名字跳过，并断言**至少构造到
一个**，使「什么都没检查」不可能通过。

### 5.4 反向注入验证（证明新门禁真的有效）

把 wasm 的 `family()` 改回 `Desktop`：

```
$ cargo test ... docs_paths_tests::documented_matrix_matches_real_backends
test ... FAILED
thread 'main' panicked at src/docs_paths_tests.rs:383:9
```

恢复后：

```
test docs_paths_tests::documented_matrix_matches_real_backends ... ok
```

**旧门禁对该缺陷恒通过，新门禁必然失败**——这正是本轮要修的那一类根因。

---

## 6. 🔴 跨目标门控收束：让「门控放底层」名副其实

第 3 节的探针修复引入了新的门控需求，本轮把门控**收敛到最底层并统一依据**：

### 6.1 发现的门控不一致（逐个实测）

| 模块 | 原门控 | 问题 |
|---|---|---|
| `platform/ios/platform_impl.rs` | 无（`ios/mod.rs` 无条件声明） | Apple 探针在非 Apple 目标上编译 → OpenHarmony `mini` 3 个 E0433 |
| `platform/ios/types.rs` | 无 | 与 `platform_impl` 分离后成为孤儿 → 5 个 `never constructed/read` warning |
| `platform/macos_objc2/` | `feature = "macos"\|"cocoa-legacy"` | 在 Windows 目标上仍编译 → 3 个 E0433 |
| `platform/mobile.rs` 探针 | 直连 `darwin_probes` | Windows/OHOS 上 E0433 |

### 6.2 处置：**门控下沉到探针模块**，调用方零 `cfg`

这是本轮对原则 #36「OS 相关逻辑落到底层」的具体落实。做法：

1. `darwin_probes` 自身 `#[cfg(target_vendor = "apple")]` —— 依据是**内核**（Darwin），
   不是 OS 列表；
2. `ios/{platform_impl,types}` 同样按 `target_vendor = "apple"` 门控，
   与 `darwin_probes` **同一个判据**（不再出现第二套条件表达式）；
3. `platform/types.rs` 新增 **运行时无关的三对分发函数**，
   把「本机内核选哪个探针」这件事收在一处：

```rust
pub(crate) fn host_memory_probe() -> Option<u64> {
    #[cfg(target_vendor = "apple")]
    { crate::platform::darwin_probes::total_memory_mb() }
    #[cfg(not(target_vendor = "apple"))]
    { crate::platform::os_probes::total_memory_mb() }
}
```

`mobile.rs`（Android 适配器，却要在任意宿主上编译以便单测）只调 `host_*_probe()`，
**自身一个 `cfg` 都没有**。`macos_objc2` 加 `target_vendor = "apple"` 与其
`platform_impl` 的探针依赖对齐，并在模块文档中写明代价：

> Known trade-off: this means the module's own state-machine unit tests do not run on
> a non-Apple host. That is the honest cost of the probe change.

（不隐瞒：这是用「非 Apple 宿主上不跑状态机单测」换「探针正确」。）

### 6.3 定点证据：四目标 × 五 profile 全部 0 error / 0 warning

| target | desktop | tablet | mobile | mini | embedded |
|---|---|---|---|---|---|
| host（aarch64-apple-darwin） | ✅ | ✅ | ✅ | ✅ | ✅ |
| `aarch64-apple-ios` | — | — | ✅ | ✅ | — |
| `x86_64-pc-windows-gnu` | ✅ | — | ✅ | ✅ | ✅ |
| `aarch64-unknown-linux-ohos` | (见下注) | — | ✅ | ✅ | ✅ |
| `wasm32-unknown-unknown` | — | — | — | — | ✅ |

> **注**：OHOS 的 `desktop` 失败于 `minimp3-sys` 的 `'stdlib.h' file not found`——
> 交叉 sysroot 缺 C 头文件，属**环境**问题（与前一轮记录一致），不是本仓库代码缺陷；
> 同一 target 的 `mobile`/`mini`/`embedded` 均通过，说明门控本身是对的。

### 6.4 顺带修掉的两处

- **`tests/zz_audit_probe_test.rs`**：上一轮遗留的探针文件，文件头自己写着
  `Throwaway audit probe. DELETE ME.`，且 `use rust_widgets::menu_config::...` 未加
  feature 门控 → `mini`/`embedded` 构建直接失败。其覆盖的
  `UserOverrides` 七字段 round-trip **在 crate 内确无等价测试**（`tests.rs` 只测
  setter 与钳制），因此不是删除而是**升格**为
  `menu_config::tests::persistence_round_trips_every_user_override`（含 `clear()`
  真的删文件），再删原文件。
- **Windows `has_print_support`**：原实现 `Command::new("cmd").args(["/C", "print /? 2>NUL"])`
  并以 `status.success()` 判定。两个问题：`print` 是 `cmd` **内建**不是可执行文件，
  `2>NUL` 是 `cmd` 方言重定向；更重要的是**在外层进程里问自己的事**——`spawn_print_job`
  走 `Start-Process -Verb Print`，那是**本进程**的环境解析的动词，问子 `cmd` 答的是
  子进程的事实。改为 `windows_shell_spawnable()`：测**能否 spawn**
  （原则 #38 的判据），与 unix 侧 `unix_print_clients_available` 同一规则。

---

## 7. 🟠 控件 `enabled` 契约：不可交互的外观必须跟上

### 7.1 先修正取证范围（诚实记录）

子代理报「147/187 控件 `draw` 忽略 `enabled`」。复核后：

```
$ for f in $(grep -rln "impl Draw for" src/widget/ --include=*.rs); do
    grep -q "is_enabled()" "$f" || echo "$f"; done | wc -l
32
```

且这 32 个里多数是**容器/图表/装饰件**（`divider`、`line`、`safe_area`、
`masonry_layout`、`qr_code`、7 个 finance 图表……），对它们而言「禁用」没有可交互
语义，外观不变是**正确**的。子代理把「未引用 `is_enabled()`」等同于「本应引用」。
**按原则 #19 记录为误报，并把范围重新界定为「接受输入却不管 enabled」的控件。**

### 7.2 重新界定后确证的缺陷

以 `cupertino_segmented_control` 为最清晰样本：

```rust
// handle_event：完全没有 enabled 检查
Event::MouseRelease { pos, button } => {
    if *button != 1 || self.segments.is_empty() { return; }
    ...
    if index < self.segments.len() && index != self.selected_index {
        self.selected_index = index;
        self.value_changed.emit(index);      // ← 禁用状态下仍然发射
    }
}
```

即 `set_enabled(false)` **对该控件完全无效**：点击照收、选中照改、信号照发。
而 `LineEdit`/`Slider`/`MessageBox` 都先查 `is_enabled()`——所以这是**不一致**，
不是有意策略。绘画侧同样固定用 `Color::WHITE` / 固定灰，禁用外观不可辨。

### 7.3 修复：单一接缝 + 逐控件补门

**(a) 新增共享接缝**（避免 30 份各自的灰色字面量）：

- `Color::disabled_variant_of(color)` —— 按通道向 `DISABLED_FOREGROUND` 混色 66%，
  **保留 alpha**，因此禁用色仍可辨认原色（不像直接刷成 `DISABLED_BACKGROUND`
  会把「禁用的主按钮」和「禁用的危险按钮」变成一样的）；
- `BaseWidget::effective_foreground(enabled_color)` /
  `effective_fill(enabled_color)` —— 一行表达「禁用就换色」。

**(b) 修 `cupertino_segmented_control`**：`handle_event` 首查 `is_enabled()`；
`draw` 的轨道、滑块、文字三处都按 `enabled` 取色。

### 7.4 定点证据与反向注入

```
$ cargo test --no-default-features --features desktop --lib segmented_control
running 16 tests ... test result: ok. 16 passed
```

新增两条**行为**测试（不是"不 panic"）：

| 测试 | 断言 |
|---|---|
| `..._disabled_ignores_taps` | 禁用后点击**不**改 `selected_index` 且**不**发 `value_changed` |
| `..._disabled_renders_differently` | 禁用前后的 SVG 输出**必须不同** |

---

## 8. 复核出的误报（诚实记录）

| 子代理结论 | 复核结果 |
|---|---|
| 「147/187 控件 `draw` 忽略 enabled」 | **数字错误**：实测 32 个，且多数是容器/图表，禁用无语义。见 §7.1 |
| 「WASM 的 `total_memory_mb`/`is_on_battery` 是假探针」 | **误报**：它们返回诚实的 `None`/`false`（浏览器沙箱确实拿不到）。真正的缺陷是 `family()`/`capabilities()`，见 §5 |
| 「`charts.rs` 的 `tick as f32 / tick_count as f32` 可能除零」 | **误报**：两个函数开头都有 `let tick_count = tick_count.max(2);` |
| 「`pie_chart.rs` 的 `fill_triangle_scanline` 可能除零」 | **误报**：`total_height`/`sub_h` 在使用前已零检查并有提前 `return`/`continue` |
| 「`gpu/manager.rs:83` 的 `clamp` 是同类崩溃」 | **误报**：`QualityLevel` 是**枚举**，其 `clamp` 是自有方法，非浮点 clamp |
| 「`video/metadata.rs` 的 `duration` 可为 NaN 导致 clamp panic」 | **半误报**：`ffmpeg_decoder::duration_in_seconds` 有 `if dur > 0` 守卫，`NaN > 0` 为假故落 `0.0`。但 `duration` 是 **public 字段**，外部可写，故仍按防御性修复处理 |

---

## 9. 完成率

| 类别 | 发现 | 已修 | 完成率 |
|---|---|---|---|
| A 数值边界 abort（14 个控件 + video，单一接缝 + 6 单测 + 2 回归测试） | 15 | 15 | 100% |
| B Apple 探针接错内核（4 后端 × 4 探针 + 新模块 + 5 测试） | 17 | 17 | 100% |
| C `mini` × OpenHarmony 不编译 | 4 | 4 | 100% |
| D WASM 谎报能力 + **门禁结构性失明**（后端 + 真后端校验测试） | 2 | 2 | 100% |
| E 跨目标门控收束（4 模块 + 3 分发函数）+ 遗留探针 + Windows 打印探针 | 9 | 9 | 100% |
| F 控件 `enabled` 契约（共享接缝 + segmented_control + 2 测试） | 4 | 4 | 100% |
| **合计** | **51** | **51** | **100%** |

### 新增 / 强化的测试（防回归）

| 文件 | 作用 |
|---|---|
| `src/widget/numeric.rs` | 新增：6 条，含交叉界限不 panic、NaN 边界被忽略、声明顺序与 `std::clamp` 逐位一致 |
| `src/platform/darwin_probes_tests.rs` | 新增：5 条，钉住"测量值"而非"无 panic"；电池测试重跑 `pmset` 比对横幅 |
| `src/platform/wasm/platform_impl.rs` | 新增：`host_capabilities_are_all_false`；修正 `backend_name_and_family` |
| `src/docs_paths_tests.rs` | 新增：`documented_matrix_matches_real_backends`——**构造真后端**比对，堵住"喂回自己的声明"式恒真断言 |
| `src/widget/cupertino/core.rs` | 新增：交叉界限不 panic、NaN 边界不污染值 |
| `src/widget/cupertino/segmented_control.rs` | 新增：禁用忽略点击、禁用外观必须不同 |
| `src/menu_config/tests.rs` | 新增：`UserOverrides` 七字段 round-trip + `clear()` 真的删文件 |
| `src/core/color.rs` | 新增：`disabled_variant_of` 的 doc-test（含 alpha 保留、已是禁用色则不变） |

---

## 10. 改动文件汇总

**数值安全（A）**
- `src/widget/numeric.rs`（新增）、`src/widget/mod.rs`
- `src/widget/cupertino/core.rs`、`src/widget/display_widgets/{lcd_number,meter,scrollbar,arc,slider,progressbar}.rs`、
  `src/widget/dialog/progress_dialog.rs`、`src/widget/container_widgets/stepper.rs`、
  `src/widget/input_widgets/{spinbox,range_slider}.rs`、`src/widget/advanced_widgets/dial.rs`、
  `src/video/engine.rs`

**平台探针（B）**
- `src/platform/darwin_probes.rs`（新增）、`src/platform/darwin_probes_tests.rs`（新增）、`src/platform/mod.rs`
- `src/platform/macos/platform_impl.rs`、`src/platform/macos_objc2/platform_impl.rs`
- `src/platform/ios/platform_impl.rs`、`src/platform/ios/mod.rs`
- `src/platform/mobile.rs`、`src/platform/types.rs`（新增 3 个 `host_*_probe` + `windows_shell_spawnable`）
- `Cargo.toml`（macos/ios 段新增 `libc`）

**门控与门禁（C/D/E）**
- `src/platform/harmony/platform_impl.rs`、`src/platform/wasm/platform_impl.rs`
- `src/platform/windows/platform_impl.rs`
- `src/docs_paths_tests.rs`
- `tests/zz_audit_probe_test.rs`（删除）、`src/menu_config/tests.rs`（承接其覆盖）

**控件契约（F）**
- `src/core/color.rs`、`src/widget/base.rs`、`src/widget/cupertino/segmented_control.rs`

---

## 11. 版本升级 2.4.5 与文档同步

| 文件 | 改动 |
|---|---|
| `Cargo.toml` | `version = "2.4.4"` → `"2.4.5"` |
| `Cargo.lock` | 同步 |
| `CHANGELOG.md` | 新增 **2.4.5** 完整条目 |
| `docs/reports/CHANGELOG.md` | `cp` 同步，`check_changelog_sync.sh` 验证两份一致 |
| `README.md` / `README.zh-CN.md` | 版本徽章 `2.4.5`；实测数字刷新；链接改指本日志 |
| `cookbook/{en,zh-CN,zh-TW}` | 版本号全量刷新 |
| `include/rw_generated.h`、`examples/rust_widgets.generated.h` | 重新生成 |

---

## 12. 最终全量验证（原则 #56 —— 一次性全跑，已实跑）

见 §13。

---

## 13. 下一轮建议方向（尚未穷尽）

按原则「连续 2 轮无新发现才终止」，本轮为第 1 轮，**尚未达到终止条件**。已修完扫出的
全部 51 项。建议下一轮从以下未穷尽方向取证：

1. **`enabled` 契约的其余交互型控件**——本轮只修了 `cupertino_segmented_control`
   这一最清晰样本，并建立了 `effective_foreground`/`effective_fill` 接缝。
   `cupertino/nav_bar.rs`、`overlay_widgets/swipe_to_dismiss.rs`、`dialog/popover.rs`
   都有 `MousePress`/`MouseRelease` 分支且无 `enabled` 检查，需逐个判定
   「禁用是否有语义」后决定修或写明不适用。
2. **`macos_objc2` 的 `supports_surfaces()`**——子代理报其继承默认 `false`，而
   cocoa 后端返回 `true`，同一台机器两个答案。本轮未修（未独立复核该结论的
   完整上下文），建议下一轮用运行时探针确认后统一。
3. **`contract.rs` 的 `fallback_native_capability_contract`**——在
   `native_capability_contract()` 返回 `None` 时造出一个**全 true** 的契约。
   这与本轮修的 WASM 属同一类（合成值冒充能力），建议下一轮取证。
4. **`os-auto` 空 feature**——文档称其"按 target_os 选后端"，但全仓无一处读它。
   属原则 #47 的"名字断言了不存在的门控"。本轮未动（删 feature 影响面大，需
   与 `check_capability_feature_gates.sh` 一并评估）。
5. **语言绑定（Python/Node/C++）逐参数契约**、**`events:` 声明与真实发射点三方对齐**、
   **Android/HarmonyOS 的原生能力探针（JNI `ActivityManager`/`BatteryManager`）**。
