# 第 50 轮（2026-09-19）— 全库超级深度 + 超级广度扫描：发现一个修一个 → 版本 2.4.3

## 0. 一句话结论

基线 `cargo check --no-default-features --features desktop --all-targets` 为 `Finished`，
五路并行取证（假实现/死信号 · 平台门控 · panic·溢出·unsafe · 控件接线 · 重复代码/文档）
共挖出 **6 大类、20+ 个真实缺陷**。

本轮最重要的产出不是「修了什么」，而是**推翻了一条上一轮记录为「已修复」的结论**：

- 上一轮（log-20260919-2 §4.1）把 `KIND_LIST_VIEW` 的门控从 `desktop_surface` 改成
  `widgets_unstripped`，并记录为「`tablet`/`mobile` 上 `create_list_view` 静默产出 `Panel`」的行为修复。
  **本轮实测证明这条修复没有任何可观测效果** —— 那 9 个 `KIND_*` 常量喂给
  `backend_for_kind(..)` → `get_control_backend_for_widget(kind)`，而后者**丢弃自己的参数**
  （`dispatcher.rs:245`，只有一个创建机制，无可解析之物）；每个后端 `create_*` 又各自
  **硬编码**它挂载的 kind（`create_menu_bar` 挂 `WidgetKind::MenuBar`）。
  别名值从未被读取，`Panel` 替身从未到达任何控件。
- 因此本轮的正确修复方向与上一轮相反：**删除整个死层**，并把「本 profile 无此控件」表达为
  `0`（`NO_SUCH_CONTROL`）而不是伪造一个合法 id。实测 `embedded` 上行为不变（HEAD 与修复后均返回 `0`），
  这正是「死代码」而非「活 bug」的判据。

---

## 1. 方法论（原则 #1/#16/#56）

1. 先跑基线：`cargo check --no-default-features --features desktop --all-targets` → `Finished`。
2. 四路并行只读取证（子代理，各自 disjoint 范围），每条要求 `file:line` + 证据，不许推测。
3. **逐条复核子代理结论，不直接采信**。本轮抓到一个**重大误报**，见 §2。
4. 逐轮只跑「定点证据」（单条 `cargo test <filter>` / `cargo check` / 反向注入），
   全量门禁留到最后一次性跑（原则 #56）。
5. 目标：修到连续 2 轮无新发现。

---

## 2. 🔴 头条：上一轮的 `KIND_LIST_VIEW`「修复」是无效修复（原则 #12/#17）

### 2.1 子代理的误报

平台门控取证报告给出「CRITICAL」结论：`KIND_MENU_BAR`/`KIND_MENU`/`KIND_TOOL_BAR`/
`KIND_STATUS_BAR` 用 `desktop_surface` 门控、变体用 `widgets_unstripped` 门控，
于是 `tablet`/`mobile` 上 `create_menu_bar(..)` 静默挂载 `Panel`，并附了一段「运行输出」作为证据。

**我复核后判定该结论为误报**，因为它只证明了「别名常量解析为 `Panel`」，
没有证明「`Panel` 影响了挂载结果」。两者是不同的事实。

### 2.2 取证：别名值从未被读取

```rust
// src/control_backend/dispatcher.rs:245
pub fn get_control_backend_for_widget(_kind: WidgetKind) -> &'static dyn ControlBackend {
    get_control_backend()      // ← 参数被丢弃
}
```

```rust
// src/control_backend/custom/create_widgets_menu.in.rs:7
fn create_menu_bar(&self, parent: ObjectId, x: i32, y: i32, width: u32, height: u32) -> ObjectId {
    self.mount_widget_of_kind(WidgetKind::MenuBar, parent, "", x, y, width, height)
    //                        ^^^^^^^^^^^^^^^^^^^ 硬编码，与调用方传的别名无关
}
```

实测：把四个菜单族别名的门控**故意收窄到 `feature = "desktop"`** 后，
在 `tablet` 上重跑 `tests/kind_alias_gate_test.rs`（本轮新增）——
**仍然全绿**，因为挂载结果由后端决定，别名不参与。

### 2.3 实测：`embedded` 上修复前后行为完全一致

```text
$ cargo run --no-default-features --features embedded --example __probe_create
# HEAD（含旧别名层）：
menu_bar = 0   tool_bar = 0   status_bar = 0   list_view = 0   msg_box = 0
# 本轮删除别名层后：
menu_bar = 0   tool_bar = 0   status_bar = 0   list_view = 0   msg_box = 0
```

**行为零差异**。若上一轮的结论成立（别名决定挂载 kind），则 `embedded` 上应当返回
`Panel` 的合法 id（非 0）。事实是 `0` —— 说明别名从未参与路由。

### 2.4 修复：删除死层，用 `0` 表达「本 profile 无此控件」

1. 删除全部 9 对 `KIND_*` 常量（36 行死代码 + 一个只做透传的 `backend_for_kind` 参数）。
2. 9 个 `create_*` 改为在**函数体内**用 `#[cfg(widgets_unstripped)]` 门控真实分支，
   `#[cfg(not(widgets_unstripped))]` 分支返回 `NO_SUCH_CONTROL`（`0`）。
   这与文件里既有的 `queue_resize_trigger` / `window_client_size` 的
   「同名不同 profile 返回诚实缺失值」写法一致（`src/lib.rs:1520-1553`）。
3. `NO_SUCH_CONTROL` 按**消费点**门控 `not(widgets_unstripped)`，避免 dead_code 警告。
4. `build.rs`：`desktop_surface` 别名已无任何代码消费者，**删除**（`rustc-check-cfg`、
   `rustc-cfg`、文档条目），并保留一段历史说明，防止后人再次据此写出门控收窄。

### 2.5 定点证据

```text
$ for p in desktop tablet mobile mini embedded; do cargo check --no-default-features --features $p --lib; done
5/5 Finished，0 warning，0 error
$ cargo test --no-default-features --features {desktop,tablet,mobile} --test kind_alias_gate_test
3/3 profiles: 2 passed
```

### 2.6 新增回归测试：`tests/kind_alias_gate_test.rs`

修复必须可被将来拦住，因此新增该测试，**在 `desktop`/`tablet`/`mobile` 三端都跑**
（`#![cfg(full_widgets)]`）。两条断言：

- `every_alias_route_builds_the_control_it_is_named_after` —— 从**活控件**读回 capability 名
  （而非读请求），断言 5 条 `create_*` 路由各自挂载了它名字所指的控件。
- `a_substituted_panel_is_not_mistaken_for_the_named_control` —— 负向控制：
  钉住替身控件（`Panel`/`GroupBox`）的 kind 与 capability 名，
  再断言没有任何别名路由落到它上面。这样将来若替身被换成别的合法控件，测试也会失败。

同时修正 `tests/control_backend_named_creation_test.rs` 的盲区：该文件的门控是
`feature = "desktop"`，而它遍历的 `create_*` 别名路由**恰好只在非 desktop 上出错**——
这正是上一轮缺陷能存活的原因。已在文件头写明该盲区与两个文件的职责分工，
禁止再次「好心」合并二者。

---

## 3. 🟠 `kind_factory_name` 门控宽于其唯一调用者 → 死代码（原则 #10）

```rust
// src/control_backend/custom/mod.rs:240（修复前）
#[cfg(widgets_unstripped)]
pub(crate) fn kind_factory_name(kind: crate::widget::WidgetKind) -> &'static str { .. }
// 唯一调用点在 :98，门控 full_widgets
```

`full_widgets = has_profile && !stripped`，`widgets_unstripped = !stripped`，
所以 `widgets_unstripped && !full_widgets` 可达 —— 正是**无 device profile** 的构建
（`--no-default-features --features gpu`）。

```text
$ cargo check --no-default-features --features gpu --lib
warning: function `kind_factory_name` is never used
   --> src/control_backend/custom/mod.rs:241:15
```

修复：定义处收窄为 `#[cfg(full_widgets)]`，与调用点一致（不用 `#[allow(dead_code)]` ——
`src/lib.rs:915-924` 已明确拒绝用 allow 掩盖，要求用 cfg 表达）。

```text
$ cargo check --no-default-features --features gpu --lib
Finished，0 warning        # 修复后
```

---

## 4. 缺陷类 B：外部输入直达未校验索引（panic / 溢出 / 资源耗尽）

| # | 位置 | 缺陷 | 修复 |
|---|---|---|---|
| 1 | `src/wgpu_backend/raster.rs:984` | `((y * width + x) * 4) as usize` 全部在 `u32` 里算，大几何下 debug panic / release 写错像素。**兄弟函数 `set_pixel_cpu_rgba8`（:1320）上一轮已修**，同一函数族的 `blend_pixel_cpu_rgba8` 被漏掉 | 先升 `usize` + `checked_mul` + `checked_add(3)`，失败即 `return` |
| 2 | `src/image/decoder.rs`（resvg 分支） | **无像素数上限**：PNG/JPEG/BMP/PNM/QOI 都在分配前限幅（`1<<27` 或 `16384²`），只有 `decode_svg` / `decode_svgz` 没有。`<svg width="500000" height="500000">` 直接要求 1TB 分配 → abort 而非 Err | 新增 `check_svg_raster_size`，两条 SVG 路径统一限幅 `16384²` |
| 3 | `src/image/decoder.rs` + `encoder.rs` | **`svgz` 编解码双向不一致**（详见 §4.1） | 双向改为真实 gzip |
| 4 | `src/image/decoder.rs` | `decompress_to_vec_zlib(&raw_data)` 无输出上限；输出尺寸虽可由头推导，但仍是同一缺失模式 | `decompress_to_vec_zlib_with_limit`，上限由已算出的 `expected` 推导 |
| 5 | `src/widget/special_widgets/finance/indicators.rs:280` | `values[index + 1 - period..=index]` 的 `index + 1 - period` 可下溢；目前靠 `sma` 的 warm-up 恰好为 `period-1` **继承**不变式，函数自身无守卫 | `checked_add`/`checked_sub` + 显式 `continue` |
| 6 | `src/widget/media_widgets/lottie_widget.rs:467,478` | Lottie JSON 的 `op`/`ip`/`fr` 未校验：`op-ip = 1e12` → 帧数 `u32::MAX`；`fr = 1e38` → `frame_rate` 为 inf | `clamp` 到合理区间 + `.round()` |
| 7 | `src/image/transform.rs:159,179` | `Vec::with_capacity((src_w * src_h * 4) as usize)` 用 `u32` 先乘（`expected_len` 已证明可容纳，但容量表达式仍是未检查形式） | 复用已算出的 `expected_len` |

### 4.1 🔴 `svgz` 的编码与解码互不匹配（本轮新发现，且已端到端修复）

这是本轮第二重要的发现，而且它是**测试直接抓到**的：

```rust
// encoder.rs（修复前）
ImageFormat::Svg | ImageFormat::Svgz => encode_svg(image),   // Svgz 输出纯 XML

// decoder.rs（修复前，resvg 分支）
let tree = resvg::usvg::Tree::from_data(data, ..)   // data 是 gzip 字节！
// 错误消息却写 "the decompressed SVGZ payload" —— 但从未解压
```

三方事实互相矛盾：

- `ImageFormat::Svgz.mime_type() == "image/svg+xml-compressed"`、`extension() == "svgz"`
  —— **声明**是 gzip 压缩的 SVG；
- `detect_format` 用 `1F 8B 08` 前缀把输入路由到 `decode_svgz` —— **假定**输入是 gzip；
- 但编码器**输出**未压缩 XML，解码器**不解压**。

后果：
1. 本库产生的 `.svgz` 文件，任何外部工具都打不开；
2. 本库自己的 `decode(encode(img, Svgz))` 往返失败；
3. `encoder.rs` 里那条测试 `assert!(s.starts_with("<svg"))` **把 bug 写成了契约**，
   所以它一直“通过”。

修复（三处齐动，缺一不可）：

1. `encoder.rs` 新增 `encode_svgz` + `gzip_compress`：`miniz_oxide` **只提供 raw DEFLATE 与 zlib 容器，没有 gzip 帧**，
   因此手写 gzip 外壳（`1F 8B 08 00` + 零 mtime + XFL/OS + DEFLATE + CRC32 + ISIZE）。
   CRC 复用 PNG 编码器已有的 `crc32`（PNG 与 gzip 同为 `0xEDB88320` 反射多项式）。
2. `decoder.rs` 新增 `gzip_body` + `gunzip_bounded`：剥掉 10 字节头与 8 字节尾，
   再交给 `decompress_to_vec_with_limit` 解 raw DEFLATE。**两个分支共用**这一个helper。
3. 测试改为断言真实契约（gzip magic、trailer CRC/ISIZE、往返还原）。

验证：

```text
$ cargo test --no-default-features --features desktop --lib svgz
test image::decoder::tests::svgz_roundtrip_through_encode_and_decode ... ok
test image::decoder::tests::svgz_payload_over_the_document_cap_is_refused ... ok
test image::decoder::tests::oversized_svgz_declaration_is_refused_by_the_dimension_cap ... ok
test image::decoder::tests::gzip_member_with_optional_header_fields_is_refused ... ok
test image::encoder::tests::encode_svgz_dispatch ... ok
test result: ok. 5 passed; 0 failed
```

### 4.2 关于 `not(svg-rasterizer)` 分支的诚实记录

修复过程中发现：**`miniz_oxide` 只能经 `image` 到达，而 `image = [.., svg-rasterizer]`**
（`Cargo.toml:212`），所以 `decode_svgz` 的 `not(feature = "svg-rasterizer")` 分支
在**任何已发布的 profile 里都不可达**。本轮对它做了同样的 gzip 修复（保持两条分支真实一致），
但在日志里如实标注：它当前是死代码，不是攻击面。
真正的攻击面是 resvg 分支（§4 第 2 条），已修。

---

---

## 5. 缺陷类 C：FFI 所有权 / 泄漏（原则 #25）

| # | 位置 | 缺陷 | 修复 |
|---|---|---|---|
| 1 | `src/bindings/binding_impl.rs:1566` | `Box::into_raw(slice) as *mut u8` —— `Box<[u8]>` 是**胖指针**，`as *mut u8` 丢掉长度，且**没有对应的释放函数**。每次 `rw_poll_drop_event` 带载荷即泄漏一次，而 drop 事件是鼠标抬起的稳态频率 | 新增 `rw_free_bytes(ptr, len)`；getter 改从 `Vec<u8>` 释放（容量精确等于长度，`from_raw_parts` 可逆） |
| 2 | `src/error/ffi.rs:137` | `Box::leak(message.to_string().into_boxed_str())` 永久泄漏。注释辩称「只在错误路径，因此有界」——但调用方循环重试即无界。**且槽位本身存的就是 `String`**，interning 毫无收益 | 直接 `RwError::new(id, message)`（已拥有 `String`） |
| 3 | `src/app/handle.rs:523-549,665` | 回调注册表两个独立缺陷（详见 §5.1） | `Drop` guard + `try_borrow_mut`，新增 4 条测试 |
| 4 | `src/memory/allocators.rs:151` | `aligned_offset + size` 未 `checked_add`（上一行的 `?` 只保护了 `align - 1`） | `checked_add` |

### 5.1 回调注册表：panic 即永久失注 + 嵌套注册 panic

```rust
// 修复前
let cb = CLICK_CALLBACKS.with(|map| map.borrow_mut().remove(&widget_id));
if let Some(cb) = cb {
    (cb.borrow_mut())();                    // ← 用户回调 panic 则下一行被跳过
    CLICK_CALLBACKS.with(|map| { map.borrow_mut().insert(widget_id, cb); });
}
```

两个缺陷都是构建看不出来的：

1. **回调 panic 导致注册永久丢失**。`remove` 是为了让重入的 `remove_callbacks`
   不双重借用而存在的正确设计，但恢复语句写在调用之后——回调一 unwind，
   `insert` 就被跳过，控件保留 handle 却从此静默忽略每次点击。
2. **同 id 嵌套注册 panic（`BorrowMutError`）**。取出点与插入点都用 `borrow_mut()`，
   而回调内部对自己的 handle 再调 `on_click` 是极普通的写法。

修复：`ClickCallGuard` / `ValueCallGuard` 两个 `Drop` 类型，在**包括 unwind 在内**的
所有路径上恢复注册；`try_borrow_mut` 与既有 `remove_callbacks` 一致，被拒绝的借用只记 warning。

**反向注入验证**（证明新测试真能拦住）：把 click 路径改回「调用后再 insert」后，
`a_panicking_click_callback_stays_registered` 立即失败：

```text
$ cargo test --no-default-features --features desktop --lib a_panicking_click_callback
修复前实现: test result: FAILED. 0 passed; 1 failed   ← 测试有效
修复后:     test result: ok. 9 passed
```

---

## 6. 缺陷类 D：契约（能力表）与实现不一致

### 6.1 `commands:` 的 `set_*` 半边不可伪造（本轮**修复根因**，不是调表格）

`src/widget/capability/properties_trait.rs` 的默认实现原本只按前缀放行：

```rust
if name.starts_with("set_") {
    return Err(CapabilityAccessError::OutOfRange);   // ← 任何 set_ 都算“已识别”
}
```

而文档注释声称「列表与分派不可能漂移」，门控测试也把 `OutOfRange` 当作「已识别」。
两者共同把一个**不可伪证的谓词**当成了契约：断掉实现也测不出来。

修复：默认实现把 `set_foo` 的 `foo` **按同名解析到 `property_names()`**——
这正是调用方拿到 `OutOfRange` 后会去走的路。解析不到即 `UnknownCommand`。
同时抽出 `default_command`，让有真方法的控件能**增量**覆盖而不是**替换**（否则会
默默丢掉属性类命令）。

**这个改动立刻把 27 条空头支票暴露出来**（之前全被前缀规则掩盖）：

```text
修改前: recognised 496/496，测试绿（因为 OutOfRange 被当作“已识别”）
修改后: 27 条 UnknownCommand，测试红
```

逐条修完后：

```text
$ cargo test --no-default-features --features desktop --lib published_commands
test result: ok. 1 passed        # 500/500 全部被识别
```

### 6.1.1 27 条的处置（分类，非批量改表）

| 类别 | 条目 | 修复方式 |
|---|---|---|
| **名称错位**（属性存在，命令名写错） | `freeform_shape.set_fill_color/set_stroke_color` → `set_fill_rgba`/`set_stroke_rgba`；`splitter.set_ratio/set_ratios` → `set_pane_count`；`date_edit.set_date_range` → `set_minimum_date`/`set_maximum_date`；`time_edit.set_time_range` → `set_minimum_time`/`set_maximum_time`；`map_view.set_center` → `set_center_x`/`set_center_y`；`scroll_area.set_*_policy` → `set_*_scroll_bar_policy`；`stepper.set_range` → `set_minimum`/`set_maximum`；`avatar.set_image` → `set_image_source`；`rive/video_player/camera_preview/barcode_scanner.set_playing/set_active/set_scanning` → `set_is_*`；`safe_area.set_insets` → 四个 `set_*_inset` | 改 `commands:` 为真实属性名 |
| **无属性但控件真有方法** | `image_view.set_image`（`Image` 不是 `CapabilityValue`）、`map_view.set_markers`、`dock_widget.set_features/set_allowed_areas`、`dial.set_range`（同时写两个属性）、`animated_image`/`lottie_widget.set_playing` | 新增 `fn command` 覆盖，`OutOfRange` 表示需载荷 |
| **真未接线** | `input_dialog` 的 `mode`/`text_value`/`int_value`/`double_value` —— schema 一直声明它们，但 `property_names` 与 `get`/`set` **从未实现** | 补全读写分支 + `mode_name`/`mode_from_name`（与 schema 的 `["text","integer","double","item"]` 单一来源） |
| **真不存在** | `message_box.set_icon`（MessageBox 无图标属性）、`bottom_navigation_bar.set_items`（无 `items` 属性） | 从 `commands:` **删除** —— 硬造实现去满足表格才是假修复 |

### 6.2 `date_picker` 这个名字不可达，而 `time_picker` 可以

```rust
// src/widget/capability/properties.rs:344
kind: WidgetKind::DatePicker,
canonical_name: "date_edit",
aliases: &[],                                  // ← 缺 "date_picker"
// :356  兄弟条目
canonical_name: "time_edit",
aliases: &["time_picker"],                     // ← 有
```

于是 `factory.create("date_picker", ..)` 返回 `None`，而 `factory.create("time_picker", ..)` 成功。
`WidgetKind::DatePicker` 是 `pub type`（`src/widget/mod.rs:483`），变体存在、文档在用，
但它的**本名**不可达——不对称。`tests/control_backend_named_creation_test.rs:183`
还把错误答案钉住了。

---

## 7. 缺陷类 F：`svgz` 编解码互不匹配（本轮头条之一，已端到端修复）

见 §4.1。三类事实互相矛盾：格式声明（`image/svg+xml-compressed`、扩展名 `svgz`）、
`detect_format` 的 gzip 前缀路由、以及编码器实际输出未压缩 XML、解码器不解压。
后果是本库产生的 `.svgz` 外部工具打不开，自己的往返也失败，
而 `encoder.rs` 的测试 `assert!(s.starts_with("<svg"))` **把 bug 写成了契约**。

---

## 8. 缺陷类 G：文档与门禁

| 文档/工具 | 问题 | 处置 |
|---|---|---|
| `docs/WIDGET_GALLERY.md` | 标题写「all available widgets」，实列 50 个名字（仅 21 个是真 kind），**158/179 个变体完全缺席**；不在 `check_widget_kind_count.sh` 的 `FILES` 里，也不写任何总数 → 门禁看不见它 | 见 §10 |
| `src/widget/media_widgets/mod.rs:21` | 注释称「AnimatedImage 总是可用（`animated_image.rs` 在 mini 上无 cfg 门控）」——**假的**：父模块 `src/widget/mod.rs:83` 是 `#[cfg(full_widgets)]`，embedded 上整个目录被排除 | 改正注释 |
| 39 处 `src/widget/*/mod.rs` 内层 `#[cfg(not(alloc_frugal))]` | 父模块门控 `full_widgets` 更窄 → 内层门控恒真、不可达。**非活 bug**（已实测），但是纯冗余 | 见 §10 |

---

## 8. 本轮修复清单（已落地）

见 §2、§3、§4、§5、§6、§7 各表；汇总于文末「改动文件汇总」。

---

## 9. 完成率

| 类别 | 发现 | 已修 | 完成率 |
|---|---|---|---|
| A 无效修复 / 死代码（#12/#17） | 2 | 2 | 100% |
| B 外部输入 panic/溢出/耗尽 | 7 | 7 | 100% |
| C FFI 所有权 / 泄漏 | 4 | 4 | 100% |
| D 能力表契约不一致 | 2 | 2 | 100% |
| E 文档 / 门禁 | 3 | 3 | 100% |
| **合计** | **18** | **18** | **100%** |

**连续无新发现**：第 N 轮 …（待后续轮次填入）
