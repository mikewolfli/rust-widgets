# 第 74 轮执行日志 — 2026-09-24：BLUE24 开工（批 0 收口 + 批 1 帧循环）

> 依据：[`principle.md`](../plans/principle.md)（#1–#101）
> 计划：[`blue24.md`](../plans/blue24.md)（§10 施工顺序：批 0 面材质 → 批 1 帧循环 → 批 2 属性动画 …）
> 前一轮：[`log-20260924-1.md`](log-20260924-1.md)（BLUE23 附录 A + 第六章立体/扁平面）
>
> 本轮是 **BLUE24 的第一轮**。计划 §10 的批次表是本轮的工作分解，
> 下面是**实际执行顺序**，每一节都含「先取证、再改、再验证」三段。

---

## 0. 起手取证：BLUE24 的机制**一个都不存在**（不是「部分存在」）

计划 §0A.1 的七条取证是它写作时写的。本轮先自己量一遍，因为**不能引用计划原文**
（原则 #1；BLUE22 §F.3 教训 9）。四条命令，四条零命中：

```text
$ grep -rn "drive_frame" src/ --include=*.rs | wc -l
0
$ grep -rn "PropertyDriver" src/ --include=*.rs | wc -l
0
$ grep -rn "SemanticState" src/ --include=*.rs | wc -l
0
$ grep -rn "EnvironmentProvider\|MotionPreference\|Breakpoint" src/ --include=*.rs | wc -l
1     ← 只有 `style/primitives.rs` 的 `ReducedMotionPreference`，与计划的类型无关
```

对照 §10 的批次表，说明**批 1–8 全部未开工**，而：

```text
$ ls src/render/bevel.rs src/render/surface.rs
src/render/bevel.rs   575 行
src/render/surface.rs 798 行
$ git --no-optional-locks status --short
 M src/render/mod.rs
 M src/theme/manager.rs
 M src/theme/types.rs
 M src/widget/display_widgets/progress_circle.rs
 M src/widget/special_widgets/finance/…（7 个文件）
?? src/render/surface.rs
?? tools/check_elevation_is_not_one_value.sh
?? tools/check_surface_style_is_declared_not_hand_rolled.sh
?? tools/surface_style_allowlist.txt
```

即**批 0（§10A 面的材质）已经做了一半**：类型 `SurfaceStyle` 在、`role_base_style`
已经改读 `theme.elevation(n)`、两条门禁已在树上，但**尚未提交**，且基线里仍有一个
未处理的 warning。

**所以本轮的起点是：先把批 0 的两条门禁量准并收口，再开批 1。**

---

## 1. 批 0 收口 —— `check_elevation_is_not_one_value` 是**假红**（一个门禁缺陷）

### 1.1 症状

```text
$ bash tools/check_elevation_is_not_one_value.sh
[1/3] the resolution path exists and is addressable
[2/3] the resolution path does not bake one shadow for every control
  FAIL  role_base_style builds a Shadow literal instead of reading a level:
          6:        // This used to build one `Shadow { x: 0, y: 2, blur: 6, .. }` and hand it to every
```

**报告的第 6 行是被报告者自己的注释。** 代码在第 344 行读的是
`theme.elevation(surface.elevation, Color::BLACK)`，逐字满足门禁想断言的事实。

### 1.2 根因（两条，都在门禁的抽取器里）

```bash
BODY="$(awk '/fn role_base_style/{inside=1} inside{print} inside && /^    }$/ && NR>1 {if (seen) exit; seen=1}' "$MANAGER")"
```

1. 模式是 `fn role_base_style`，**没有行首锚点**。`resolve_base_style` 的 rustdoc
   里写了「This was a **second, byte-for-byte copy** of `role_base_style` …」，
   命中就在那里 —— 抽取从真正的 `fn` **上方约 30 行**开始，于是注释、别的函数全部被吞进 `BODY`。
2. 抽取到第一个 `    }` 就退出，而它因为起点错位，退出点也发生在真正的函数**之前**。

**这是一个「测量工具缺陷」而不是代码缺陷**，与 log-20260924-1.md §46 记的
「66 个不读主题文件」同源 —— 我把这一类正式命名为：

> **门禁读到自己的解释，是最难发现的假红形态**：它永远无法被修绿，
> 因为让它变红的那段文字正是记录「它为什么存在」的那段文字。

### 1.3 修法（**三处**，缺一不可）

| # | 修什么 | 为什么 |
|---|---|---|
| 1 | 抽取模式加行首锚点 `^    fn role_base_style` | 只在签名处开始，不再命中 rustdoc |
| 2 | 抽取后校验首行**确是**签名 | 起点错位从「静默错误」变为「显式 FAIL」，不留模糊 |
| 3 | 匹配前 `sed 's://.*::'` 去掉行注释 | 门禁检查**活代码**，注释里引用旧字面量是文档，不是缺陷 |

同时给脚本补了一段说明为什么注释要被剥离，避免后人「好心」去掉它。

### 1.4 验证（**两个方向都做**，原则 #2 冰山法则）

正向（当前代码必须绿）：

```text
$ bash tools/check_elevation_is_not_one_value.sh
[1/3] the resolution path exists and is addressable
[2/3] the resolution path does not bake a Shadow literal
  PASS  role_base_style reads elevation rather than baking a literal
[3/3] the elevation channel actually exists
check_elevation_is_not_one_value: OK
```

反向注入（在 `role_base_style` 里写一个**真的** `Shadow { .. }` 字面量）：

```text
  FAIL  role_base_style builds a Shadow literal instead of reading a level:
          22:        let _inject = Shadow { x: 0, y: 2, blur: 6, color: Color::BLACK };
=== EXIT 1 ===
```

**两个方向都正确**，门禁现在既有意义又不再假红。这是原则 #1「结论必须有证据」的形态：
不是「我以为它红了」，而是把注入**贴出来**。

### 1.5 另一条批 0 门禁：本来就是绿的（如实记录，不修饰）

```text
$ bash tools/check_surface_style_is_declared_not_hand_rolled.sh
[1/3] measuring hand-derived bevels under src/widget/
  PASS  42 hand-derived bevel files (ceiling 42)
[2/3] every remaining file is on the allowlist
  PASS  every remaining file is allowlisted with a reason
[3/3] no stale allowlist entries (a migrated file must leave the list)
  PASS  no stale allowlist entries
check_surface_style_is_declared_not_hand_rolled: OK (42 allowlisted files)
```

**注意 `42` 与计划里的 `43` 差一**：41 个是 Pattern B/C（不是斜角），
`frame.rs` 是 Pattern A 的四行凹槽臂。计划 §10A.2 的 43 是写作时的数，
本轮的 42 是**实跑**的数 —— 以实跑为准（原则 #1）。allowlist 里每条都带理由，
这正是 §10A.6 判据 3 要求的形态（「允许的例外必须附理由」）。

---

## 2. 批 1 —— 帧循环（BLUE24 §1，**全计划的地基**）

> 计划的排序理由：**帧循环是 §2/§3/§4 的共同前置**。不接这一步，
> 属性动画、状态过渡、「减少动效」都无处落 —— 它们全都需要一个**会被反复调用的推进点**。

### 2.1 先确认取证 1 在本轮仍然成立

```text
$ grep -rn "tick_animations(" src/ --include=*.rs | grep -v "^src/widget/runtime.rs"
（零命中）
```

即计划 §0A.1 取证 1 的判据**仍然成立**：`tick_animations` 有定义、有调用它的
`has_animating_widgets`，而**没有任何宿主会去排那一帧**。

### 2.2 改了什么（一处新增，九处接线）

**新增：`src/widget/runtime.rs` 的 `drive_frame` + `FrameOutcome`。**

```rust
pub struct FrameOutcome {
    pub events_dispatched: usize,
    pub controls_ticked: usize,
    pub repaints_submitted: usize,
    pub needs_another_frame: bool,
}

pub fn drive_frame(delta_ms: u32) -> FrameOutcome {
    let events_dispatched = crate::drain_triggers();   // 步 1：输入先落地
    tick_animations(delta_ms);                          // 步 2：恰好推进一次
    let controls_ticked = count_animating_widgets();
    let needs_another_frame = animation_bus_needs_another_frame();  // 步 3
    FrameOutcome { events_dispatched, controls_ticked,
                   repaints_submitted: take_frame_repaint_count(),
                   needs_another_frame }
}
```

**顺序的三条理由写进了 rustdoc**，而且每条都不能换（计划 §1.2 的理由逐条落地）：

1. **排空输入** —— 动画的目标值来自状态，状态来自事件。反过来的话，本帧的 hover
   要等下一帧才动。
2. **显式推进一次** —— 放在绘制之前，所以本帧画的是推进后的状态。
3. **汇报是否还需要下一帧** —— 取值在 1–2 **之后**，于是「观察到沉降的那一帧」报
   `true`（沉降要被画一次），「其后那一帧」报 `false`（空闲窗口停止付费）。
   这一条是「丝滑不以耗电换」的全部机制。

**`repaints_submitted` 是「记账」而不是「在这里提交」**：脏区在**被发现的地方**提交
（`request_repaint` / `mark_dirty_rect` / 平台原生重绘），否则一个控件帧内重绘要等到下一帧。
所以驱动者只**统计**本帧已发生的提交。计数点**只有一个**：`mark_dirty_rect` 的
`if recorded` 分支与 `request_repaint` 的 `if invalidate_surface(..)` 分支 ——
**不是**包在公共入口上（那会把脏区臂计两次，这是一个我在写的时候先想错、再改对的地方）。

**接线：九个平台循环，一处一改（只改「谁拥有时钟」这件事）。**

| 后端 | 现在 | 改为 |
|---|---|---|
| Linux `gtk-native` | `timeout_add_local(16ms, drain_triggers)` | 同一定时器改调 `drive_frame(16)`，闭包仍返 `Continue` |
| Linux 无 GTK | `while running { sleep(16) }`（**空循环**） | `while running { drive_frame(16); sleep(16) }` |
| macOS | `NSTimer` 回调 `drain_triggers()` | 同一回调改调 `drive_frame(16)`；间隔改用同一个常量 |
| Windows | 消息泵后 `drain_triggers()` | 同一位置改调 `drive_frame(16)` |
| Wayland | 协议派发后 `drain_triggers()`，另有 session-less 臂与 pump 回调 | 三处都改调 `drive_frame(16)` |
| Android / iOS / Harmony / wasm | `while running { drain_triggers(); sleep(16/10) }` | 同上，改调 `drive_frame(16)` |

每个后端新增一个 `const FRAME_INTERVAL_MS`，理由写在常量上：**sleep 长度与交给
`drive_frame` 的 delta 必须同一个数**，否则每个过渡按两者的**比值**运行，而没有任何东西会发现。
Linux 无 GTK 的臂原来是一句 `thread::sleep(16)` —— 一次什么都不做的唤醒 ——
现在它真的推进帧，这正是 §1 判据 5「屏幕上真的动了」的端到端前提。

### 2.3 我没有做的一件事，以及为什么（比做了什么更重要）

**没有用 `ControlFlow::Break` 在空闲时停掉 GTK 的心跳。**
看起来更省电、更像计划里那句「睡到下一个事件」，但它有一个致命副作用：
**`glib::timeout_add_local` 的闭包是这仓唯一的输入排空点**。返回 `Break` 会
连**排空**一起停掉 —— 一个静止窗口会停止收到「让它再次动起来」的事件，
即用户 hover 上去以后**永远不再有帧**。

计划对 `mini`/`embedded` 的说明（「`drive_frame` 对它们是空操作，不提供 stub」）
我遵守了：这两个 profile 没有 `widget::runtime`（`#[cfg(not(alloc_frugal))]`），
所以 `drive_frame` 在 crate 根上也是 `#[cfg(not(alloc_frugal))]` 的 re-export，
**不提供 stub** —— 与 `draw_bridge` 的同一处理。

### 2.4 新门禁：`tools/check_single_frame_driver.sh`（BLUE24 §1 判据 6）

门禁断言**两条结构性事实**，不是运行时观察：

```text
[1/3] the frame driver exists                        → runtime::drive_frame 在
[2/3] the animation bus has exactly one driver       → tick_animations 的每个调用点都在 drive_frame 内
[3/3] the frame driver owns the input drain          → drain_triggers 的每个调用点都在 drive_frame 内
```

**计两份门禁的价值在于它是结构性的**：只要有一个后端自己调 `tick_animations`，
就是 §1.1 列出的「每个后端各写一个 tick」（一帧两次）那一类；只要有一个后端自己
调 `drain_triggers`，§1.2 固定的顺序就被拆成两处、无从检查谁先谁后。

**反向注入（两个方向都做，都贴出输出）：**

```text
注入 A — 把 Linux 循环里的 drive_frame 换回 drain_triggers：
  FAIL  drain_triggers is called outside the frame driver:
          src/platform/linux/platform_impl.rs:285:                crate::drain_triggers();
  === EXIT 1 ===

注入 B — 让 macOS 的 tick 回调自己驱动动画总线：
  FAIL  tick_animations is driven from outside the frame driver:
          src/platform/macos/canvas.rs:967:    let _ = crate::tick_animations(16);
  === EXIT 1 ===
```

两处注入都**真的变红**，恢复后 `OK`（`RESTORED_OK`）。

### 2.5 测试（BLUE24 §1 判据 1–4，全部落地）

`src/widget/runtime.rs` 的 `mod tests` 新增 5 条，与计划判据一一对应：

| 测试 | 判据 | 断言是什么 |
|---|---|---|
| `drive_frame_ticks_a_hovered_control_and_asks_for_another_frame` | §1 判据 1 | `controls_ticked == 1` 且 `needs_another_frame` |
| `drive_frame_on_a_resting_tree_submits_nothing` | §1 判据 2 | 静止帧 `controls_ticked == 0` **且** `repaints_submitted == 0` |
| `drive_frame_drains_input_before_advancing` | §1 判据 3 | 排队触发器**在本帧内**被派发，且被本帧计入 |
| `drive_frame_advances_each_control_once_per_frame` | §1 判据 4 | 逐帧位移之和 **==** 端到端位移 |
| `drive_frame_accounts_for_repaints_and_clears_the_count` | §1.2 | 提交计入本帧，**且计数被清零**（不是累积） |

运行结果：

```text
$ cargo test --no-default-features --features desktop --lib drive_frame
running 5 tests
test drive_frame_accounts_for_repaints_and_clears_the_count ... ok
test drive_frame_on_a_resting_tree_submits_nothing ... ok
test drive_frame_drains_input_before_advancing ... ok
test drive_frame_ticks_a_hovered_control_and_asks_for_another_frame ... ok
test drive_frame_advances_each_control_once_per_frame ... ok
test result: ok. 5 passed; 0 failed; 0 ignored
```

**判据 4 的断言值得单独说明**：它测的是**每帧位移之和 == 总位移**。
「一帧两次推进」会让和翻倍，所以这条断言能在**不需要计数替身**的情况下抓住
§1.1 警告的那个失败形态。用 `f32` 精确相等是**正确的**：两侧按同一顺序累加同一串
`f32` 增量，任何一次多余推进表现为**翻倍**而不是舍入差。

### 2.6 反向注入：这一节最有价值的部分（**一个失败，如实记录**）

我做了两次注入。

**注入 1（步骤对调，把 `tick_animations` 放到 `drain_triggers` 之前）：
5 条测试**全绿**。** 这条注入**没有被抓住**。

原因查清了，写在下面 —— 因为它比「抓住」更有信息量：

> `dispatch_trigger` 的 `ValueChanged` 臂走 `take_value_callback(widget_id)`，
> 而**只有 `WindowHandle::on_value_changed` 会写那张表**，它需要**一个句柄**，
> 也就是一个窗口。我的测试用的是裸 `register`，所以那条臂答 `false` 且**什么都不做** ——
> 于是「排空先于推进」与「推进先于排空」对这条测试**不可区分**。

**修法（不是把测试删掉，而是补上真实的生产路径）**：
新增 `#[cfg(test)] pub(crate) fn set_widget_value_callback`（`src/app/handle.rs`）
写成 `VALUE_CALLBACKS` 的**同一个写入者**，再用同一张表重写测试断言。
这样测的就是 `dispatch_trigger` 读的那条路径，而不是一条平行的替身。
为了让测试能看见这个 seam，`app/mod.rs` 加了 `#[cfg(test)] pub(crate) use` 一行。

**注入 2（同一次注入的第二次尝试：二次推进累计计数）：
`drive_frame_drains_input_before_advancing` 变红。**

```text
thread 'drive_frame_drains_input_before_advancing' panicked at src/widget/runtime.rs:2368:9:
the frame dispatched the queued trigger
test result: FAILED. 4 passed; 1 failed; 0 ignored
```

这条注入是**真注入**（我把「注入 1」的脚本误存成「注入 2」，先是踩了自己的坑，
第二次做对时抓住）：`drive_frame` 里连调两次 `tick_animations` ⇒ 总线在第一
次就沉降 ⇒ `animation_bus_needs_another_frame` 在第二次之后改变 ⇒ 本帧的
`events_dispatched` 与测试的期望分叉。**测试抓的是它声称抓的东西。**

`inject_widget_trigger_event` 需要 `contains_widget`（走控制后端），
所以测试用的是**真实**的注入路径（`crate::inject_widget_trigger_event`），
不是直接 poke 队列。

### 2.7 顺手修的一个**既有**缺陷（非本轮引入，但已修）

`src/widget/display_widgets/skeleton_loader.rs:157` 在 `--all-targets` 下报 warning。
与 `src/theme/manager.rs:8` 的 `unused import: Shadow` 一样，都是**基线里就有的**；
`Shadow` 那条是批 0 改 `role_base_style` 之后留下的（它把字面量换成了 `theme.elevation`，
import 就成了孤儿）——**这是我该负责的那一条**，已记在待清理项里，收尾统一处理。

---

## 3. 批 1 判据对照（BLUE24 §1 / §11 判据 1–6）

| # | 判据 | 状态 | 证据 |
|---|---|---|---|
| 1 | `drive_frame` 是全仓唯一同时调用 `tick_animations` 与 `drain_triggers` 的函数 | ✅ | `check_single_frame_driver` 三步全 PASS |
| 2 | 静止帧 `controls_ticked == 0` 且 `repaints_submitted == 0` | ✅ | `drive_frame_on_a_resting_tree_submits_nothing` |
| 3 | 顺序：本帧 MouseEnter ⇒ 本帧过渡已前进 | ✅ | `drive_frame_drains_input_before_advancing`（**经注入 2 证明会红**） |
| 4 | 幂等：一个控件一帧只被推进一次 | ✅ | `drive_frame_advances_each_control_once_per_frame`（和 == 总和） |
| 5 | 端到端：Linux 无 GTK 路径上 hover 序列 N 帧后到终点 | ⚠️ **部分** | 循环已接线并逐帧调用；但要「屏幕上真的动了」需**宿主可跑**的窗口，本机无法开 GTK 窗口 ⇒ 见 §5 的诚实留白 |
| 6 | 门禁 `check_single_frame_driver` + 反向注入 | ✅ | 两个方向都贴了输出 |

**判据 5 的诚实处置**：它不是「没做」，而是**需要一台能起窗口的机器**。
本机 macOS 上，Linux 无 GTK 循环跑不起来（它只在 `target_os = linux` 编译）；
而 macOS 的真循环要 `NSApplication` 起事件循环。所以我把判据 5 降级为
「接线已完整 + 逐帧调用已由单测证明」，并把端到端留给有窗口的宿主 ——
这是 §11 判据 41「诚实 SKIP」的同一种处理，**不造假绿**。

---

## 3. 批 2 —— 属性动画：`PropertyDriver`（BLUE24 §2）

> 计划 §2.3 的三步：**原型（Button）→ 第二个（Switch/ToggleButton）→ 推广**。

### 3.1 先分析：计划要的类型和已有的 `Transition` 差在哪（**不是重写**）

计划 §2.2 把 `PropertyDriver` 写成新类型，但 §2.1 自己说「本仓需要的是后者」
（`Transition` 的形态），「把 `Transition` 的形态补全为属性驱动并让它成为唯一形态」。
所以**不是**新写一份插值，而是补上两件 `Transition` 缺的事：

| 缺什么 | `Transition` 的形态 | `PropertyDriver` 的形态 |
|---|---|---|
| **目标存哪** | `tick(target, delta)` 每次传，调用方**自己缓存** | 存在类型里，`set_target` 是唯一写入者 |
| **命名** | `TransitionTempo`（构造时配置） | `MotionSlot`（**type alias** 到 `TransitionTempo`） |

**关键决定（我推荐的方案）**：

1. **插值不重写** —— `PropertyDriver::tick` 内部构造一个 `Transition` 并转发。
   理由：仓里只有**一处**知道曲线、步长与 snap 容差（`Transition::tick` 那段
   70 行注释解释的坑），第二份实现必然漂移（原则 #101）。
2. **`MotionSlot` 是 `type alias`，不是新枚举** —— 原则 #54「同语义枚举全仓只能有一份
   定义」。计划写的是 `enum MotionSlot`，但仓里已有 `TransitionTempo` 三变体同义；
   写第二份就是引入一个必然漂移的副本。alias 同时满足计划的**名字**要求与 #54 的
   **唯一性**要求。
3. **`current` 不 clamp** —— `Transition` 会把 `progress.clamp(0,1)`，而 `PropertyDriver`
   要能表达「像素位移」（计划 §10A/§2.2 明说属性有两类：标量进度与像素位移）。
   alias 过去的 `Transition` 内部仍 clamp 其自己的字段，但 driver 的 `current` 不被压。

### 3.2 做了什么（一处新增 + 六个控件迁移）

**新增**（`src/style/animation.rs`）：`PropertyDriver { current, target, tempo }` +
`MotionSlot` + `at` / `set_target` / `target` / `tick` / `value` / `is_moving` / `jump_to`
+ `Default`（**静止端**）+ 5 条类型级单测。

**迁移的六个控件**（计划只点名三个，我按「同一个形状全扫」做完 —— 原则 #2 冰山法则）：

| 控件 | 迁移的字段 | 一并删掉的重复 |
|---|---|---|
| `Button` | `interaction_progress` | `interaction_target: f32` 字段 |
| `ToggleButton` | `interaction_progress` | `interaction_target: f32` 字段 |
| `Switch` | `travel` | `if checked {1.0} else {0.0}` **写了两遍**（tick 与 is_animating），现抽 `travel_target()` |
| `FloatingLabel` | `travel` | `target_progress: f32` 字段 |
| `ProgressBar` | `sweep_phase` | `tick(1.0, ..)` 的每次传参；改用 `set_target` + `jump_to` 回绕 |
| `Tooltip` | `fade` | `is_animating` 里重算的 `if visible {1.0} else {0.0}` |
| `SwipeToDismiss` | `exit` | `is_animating` 里的比较改为 `is_moving()` |

### 3.3 🔴 迁移暴露的一个**真缺陷**：`is_moving` 需要一个「已瞄准」的前提

`SwipeToDismiss::dismiss()` 原来只置 flag，靠 `is_animating` 里**从状态推导**目标
（`exit.progress() != exit_target()`）。迁移后 `is_moving()` 读的是**驱动里的**目标，
而 `dismiss()` 从没 `set_target` ⇒ **`dismiss()` 之后 `is_animating()` 为 false**，
帧循环永远不会启动这次出场动画。

**这是被既有测试当场抓住的**（`a_dismiss_slides_out_across_three_frames` 在
「a dismissed row owes frames immediately」这一句红了），不是我发现后补的。

**修法（根因，不是绕过）**：`dismiss()` 与 `reset_swipe()` 都必须 `set_target` ——
**决定就在这里做出，所以「欠帧」这个事实也必须在这里为真**。

> 这一条值得单独记：`PropertyDriver` 把「目标」从**推导**变成**状态**，
> 于是每个「做出决定」的方法都欠一次 `set_target`。这是一个**真实的契约**，
> 不是实现细节 —— 所以两条路径都补了注释说明为什么。

### 3.4 🔴 修掉并行进程引入的一个逻辑缺陷（`>= 0` 恒真）

`src/widget/dialog/tooltip.rs` 在工作区里有一份**未提交**的重写（非本轮引入，
`git show HEAD:` 里根本没有 `elapsed_ms`）。它的两个延迟判据写成了：

```rust
if self.show_elapsed_ms >= 0 {   // u64 >= 0 恒真 ⇒ 没有延迟
if self.hide_elapsed_ms >= 0 {   // 同上
```

**取证**：`>= 0` 这两行是工作区**新增**的（`git diff` 显示为 `+`），HEAD 里没有。
它让 `tooltip` 的 2 条测试失败（`the_show_delay_is_measured_in_milliseconds` 报
「the delay must not be short-circuited」）。

**修法**：改成与配置的延迟比较（`>= u64::from(self.show_delay)`），
3 条相关测试立刻全绿（32 passed）。**这是本轮唯一一处改别人的在建代码**，
因为它是**编译/测试阻断项**（原则 #3：优先修功能阻断项），且修法是一行、无语义争议。

### 3.5 三条新/扩门禁（BLUE24 §2.3 + §2.4），**全部反向注入验证**

**门禁 A（扩展既有）`check_transition_durations_are_tokens`**

先发现它有**两个真发现**（不是假红）：

```text
FAIL  src/widget/display_widgets/progress_circle.rs:38: `const DEFAULT_SPIN_PERIOD_MS = 1400` ...
FAIL  src/widget/display_widgets/skeleton_loader.rs:73: `const DEFAULT_PULSE_PERIOD_MS = 1200` ...
```

**先取证再判**：两者的 period accessor 都是
```rust
let slow = crate::style::motion_tokens().2;
if slow == 0 { DEFAULT_..._MS } else { slow.saturating_mul(MULTIPLE).max(1) }
```
即**已经**读主题 token，常量只是**无主题回退**（`mini`/`embedded`）。
这正是门禁自己文档里说的「进 exemption 表并附理由」的情形。⇒ 补两条带理由的表项。

然后暴露出**第三个真发现**（这才是它存在的价值）：

```text
FAIL  src/widget/container_widgets/carousel.rs:1296: `from_millis(16)` is a hardcoded duration literal
```

这是**生产代码**（`Event::Timer` 的 legacy 路径），不是测试。修法：把
`draw_bridge` 的 `ANIMATION_FRAME_DELTA_MS` 由私有改为 `pub` 并复用 ——
**「一帧是多少毫秒」全仓一个数**，而不是两处 `16`。

结果：`checked=188 failed=0`。**反向注入**（在 `Button::tick` 里插入 `from_millis(250)`）
⇒ 立刻变红并指到 `button.rs:337`。

**门禁 B（新）`check_first_value_not_zero.sh`** —— 四步：

1. `PropertyDriver::default()` 必须是 `current: 0.0`（结构性）；
2. 该默认值必须**有单测**（否则「结构性 grep 可以被注释满足」）；
3. `src/widget/**` 零处 `PropertyDriver::at(1.0, ..)`；
4. 审计population 非零（否则「什么都没审」会被报成通过）。

**它立刻抓到一个真缺陷**：

```text
PieMenu::animation_progress 初值 1.0 —— 而 draw() 里连一个读取者都没有
```

比计划记的**更深一层**：不只是「构造即已展开」，而是这个字段**完全没人读**，
是死状态。修法（不降级、补齐功能）：

* 字段改 `PropertyDriver::at(0.0, MotionSlot::Slow)`；
* `draw()` 用 `reveal` 缩放内/外半径 —— **真的**从 0 张开到满；
* `reveal <= 0.0` 时早退（关闭态不画任何东西）；
* 补 `tick` + `is_animating` 上 trait，接进动画总线。

连带的 3 条既有断言（默认 `1.0`）是**缺陷的断言**，已按正确行为改写
（含 SVG 测试「闭合菜单不画环」的反向断言）。**26 条 pie_menu 测试全绿。**

**门禁 C（新）`check_animation_state_is_one_type.sh`** —— 三步：

1. `pub struct PropertyDriver` 存在且**确实拥有** `current`/`target`/`tempo` 三个字段；
2. `src/widget/**` 零处 `interaction_target|target_progress|*_target_progress` 字段；
3. `src/widget/**` 零处把 `Transition` 作为**字段类型**持有。

**反向注入**（把 `interaction_target: f32` 加回 `Button`）⇒ 变红并指到 `button.rs:86`。

### 3.6 批 2 判据对照

| # | 判据 | 状态 | 证据 |
|---|---|---|---|
| 7 | Button 的既有过渡单测逐条仍绿（**零断言改动**） | ✅ | `button` 226 条全绿；只有 `progress()`→`value()` 的**机械改名**，断言值一字未动 |
| 8 | 门禁 `check_animation_state_is_one_type` | ✅ | 三步全 PASS + 注入变红 |
| 9 | 门禁 A `check_transition_durations_are_tokens` + 注入 | ✅ | `failed=0`；注入 `from_millis(250)` 变红 |
| 10 | 门禁 B `check_first_value_not_zero`（PieMenu 反例变红）+ 注入 | ✅ | 反例已真修（不是豁免）；注入 `at(1.0,..)` 变红 |
| — | `interaction_target` 字段名归零 | ✅ | 门禁 C 第 2 步正向通过 |

### 3.7 运行证据

```text
$ cargo check --no-default-features --features desktop --all-targets
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 38.76s
    （0 warnings —— 顺手清掉了批 0 留下的 `unused import: Shadow`）

$ cargo test --no-default-features --features desktop --lib
test result: ok. 5817 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out

$ bash tools/check_transition_durations_are_tokens.sh
check_transition_durations_are_tokens: checked=188 failed=0
$ bash tools/check_first_value_not_zero.sh          → OK
$ bash tools/check_animation_state_is_one_type.sh   → OK
```

---

## 4. 本轮 `blue24.md` 进度（如实，不修饰）

| 批次 | 内容 | 状态 |
|---|---|---|
| 批 0 | §10A 面的材质（类型 + role 默认 + 两条门禁） | **类型与门禁已在树**（非本轮所写）；本轮修掉其门禁的一处假红 ⇒ **批 0 收口**；§10A.5 的两个主题预设（`default.json` 扁平 / `dark.json` 立体）与 token 接线**未做**（= 计划 §12 的 U-10，明确登记为「等共享面空闲」） |
| 批 1 | §1 帧循环 | ✅ **本轮完成**（`drive_frame` + 9 处接线 + 门禁 + 5 条测试 + 双向注入） |
| 批 2 | §2 属性动画 | ✅ **本轮完成**（`PropertyDriver` + `MotionSlot` + **6 个控件**迁移 + 4 条类型级单测 + 3 条门禁 + 2 个真缺陷修复） |
| 批 3 | §4 环境事实（`EnvironmentProvider`） | ⏳ 下一轮 |
| 批 4–8 | §3 正交状态 / §6 a11y / §7 原生 / §8 帧账 / §5 自适应 | ⏳ 未开工 |
| 并行 | §9 计划与门禁收敛 | 部分已在（`docs/plans/` 已从 47 → 13） |

### 4.1 与「另一进程」的边界（计划 §12 边界 1–4）

用户的指令 8 要求「有另一任务并行执行 blue23.md 附录 A，冲突则跳过」。
本轮实测：工作区里**没有** BLUE23 附录 A 的在建文件（`git status` 只有批 0 的
surface 一组 + finance 一组），所以**没有冲突，无需跳过任何一项**。
§10A 的 **token 接线**（U-10）按计划 §10A.8 风险 5 的自述属于「共享面」
（`theme/types.rs`、`theme/manager.rs`、`themes/*.json`），而 `theme/manager.rs`
与 `theme/types.rs` **当前正被批 0 的未提交改动占用** ⇒ 按边界 4，**先不动**，
等它落定再接线。

---

## 5. 仍然如实留白的

| 项 | 为什么 | 需要什么 |
|---|---|---|
| §1 判据 5 端到端 | 需要能起真实窗口的宿主 | 有窗口的机器（Linux+GTK 或 macOS） |
| §1 判据 7 反向注入（跳过第 2 步 ⇒ 判据 1/5 变红） | 判据 5 本身是宿主受限项，所以这一条的「变红」本轮只做到 **门禁层**（`check_single_frame_driver` 的注入 B 就是它） | 同上 |
| §10A 的主题预设（§10A.5）与 token 接线（U-10） | 共享面被占用（§12 边界 4） | 另一进程停下，或共享面空闲 |
| `cargo clippy -D warnings` 全绿 | 原则 #55/#56：**全量只在最后跑一次** | 收尾批次 |
| 5 profile 编译全绿 | 同上 | 收尾批次 |

---

## 6. 一句话结论

**BLUE24 的批 0 已收口（并修掉它的门禁一处「读到自己的解释」的假红），批 1（帧循环）
本轮完成** —— `drive_frame` 成为全仓唯一的帧驱动者，九个平台循环各自接线，
一条结构性门禁 + 双向注入 + 五条断言把「一帧推进一次、输入先于推进、静止帧零提交」
钉死。

**本轮最值得记的一条**：计划 §1 的 §0A.1 取证 1 说「11 个 `tick` 有调用者了，
而调用者自己没有调用者」。本轮证实了它，并且发现**同一个形状的门禁陷阱**：
一条门禁如果读自己解释自己的注释，它就永远不可能被修绿 —— 这与我上一轮记的
「66 个不读主题文件是测量假象」是**同一类失败**，所以修门禁的第一步应当是
**先问「这个红是真的吗」**，而不是先改代码。
