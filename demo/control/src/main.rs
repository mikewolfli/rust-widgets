//! Control Demo 入口桩（stub）。
//!
//! ╔══════════════════════════════════════════════════════╗
//! ║  设计约定：main() 只调用 app::run()                ║
//! ║  所有业务逻辑、窗口创建、控件构建放在 app 模块中    ║
//! ╚══════════════════════════════════════════════════════╝
//!
//! 理由：
//! - main 作为启动桩，职责单一：初始化模块 + 启动 app
//! - app 模块作为入口层，管理应用生命周期
//! - 便于单元测试：可直接调用 app::run() 或 app 内部函数
//! - 保持一致的项目结构：所有 demo 统一使用此模式

mod app;

fn main() {
    app::run();
}
