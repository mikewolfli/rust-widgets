# Widget Gallery Generator

Generates SVG renderings of all widgets and an HTML gallery page.

## Usage

```bash
cargo run --example widget_gallery [output_dir]
```

Default output directory: `widget_gallery/`

## Output
- `index.html` — Gallery page with category groupings
- `*.svg` — Individual widget SVG renderings
