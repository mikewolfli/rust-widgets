//! Visual rendering helpers for WebEngine widget types.
//!
//! **Phase 2c cleanup**: All `append_web_engine_*_visual_commands` functions,
//! `push_widget_fill_and_border`, and the `#![allow(dead_code)]` attribute were
//! removed. The widget-type imports were also cleaned out.
//!
//! This module is retained as a placeholder to preserve the `pub(crate) mod engine`
//! declaration in `render/web/mod.rs`. Re‑implement when WebEngine integration is
//! actually wired into the rendering pipeline.
