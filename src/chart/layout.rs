//! Cartesian layout computation and axis/legend drawing.
//!
//! **Phase 2c cleanup**: All functions were duplicated in `charts.rs` (the active version).
//! This module is retained as a placeholder to preserve the `pub mod layout` export
//! in `chart/mod.rs`. Remove this file and the `pub mod layout;` declaration once
//! the migration to `charts.rs` is fully validated.
